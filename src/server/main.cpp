//#include "Server.h"

int main()
{
	//Server* mpServer = new Server("200");

	// run the program as long as the server is running
	while (true)
	{
		//mpServer->update();
	}

	return 0;
}